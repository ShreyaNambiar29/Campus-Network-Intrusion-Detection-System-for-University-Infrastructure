import React from "react";
import { createRoot } from "react-dom/client";
import CampusIDS from "./CampusIDSDashboard.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CampusIDS />
  </React.StrictMode>
);
