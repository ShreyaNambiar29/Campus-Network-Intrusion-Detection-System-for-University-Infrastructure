{
  "name": "campus-ids-frontend",
  "version": "1.0.0",
  "description": "Campus Network Intrusion Detection System - Frontend Dashboard",
  "scripts": {
    "start": "python -m http.server 3000",
    "dev": "live-server --port=3000 --open=/index.html",
    "build": "echo 'Static build - no build step required'",
    "deploy": "echo 'Deploy to Vercel or Netlify'"
  },
  "keywords": [
    "cybersecurity",
    "intrusion-detection",
    "network-security",
    "dashboard",
    "ids"
  ],
  "author": "Campus IDS Team",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
